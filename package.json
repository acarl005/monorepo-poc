{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "lerna": "^2.3.1"
  },
  "scripts": {
    "publish": "lerna publish",
    "build": "lerna run build",
    "clean": "lerna run clean && find . -name node_modules -type d -exec rm -r '{}' \\;"
  }
}
